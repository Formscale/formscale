export enum SubscriptionTier {
  FREE = "free",
  PRO = "pro",
  BUSINESS = "business",
  ENTERPRISE = "enterprise",
  BENEFIT = "benefit",
}

export enum Roles {
  ADMIN = "admin",
  USER = "user",
}
