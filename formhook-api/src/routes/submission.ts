// import { Hono } from "hono";
// import { getSubmissions, createSubmission, getSubmission, updateSubmission, deleteSubmission } from "../controllers/SubmissionController";

// const submissionRoutes = new Hono();

// submissionRoutes.get("/form/:id/submissions", getSubmissions);
// submissionRoutes.post("/form/:id/submission", createSubmission);
// submissionRoutes.get("/form/submission/:submissionId", getSubmission);
// submissionRoutes.patch("/form/submission/:submissionId", updateSubmission);
// submissionRoutes.delete("/form/submission/:submissionId", deleteSubmission);

// export default submissionRoutes;
